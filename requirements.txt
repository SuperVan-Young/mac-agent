# Unified Python dependencies for this repository.
# Installed by env/setup_conda.sh via:
#   conda run -p <prefix> python -m pip install -r requirements.txt

# xDSL compiler stack
xdsl>=0.36.0
networkx>=3.2

# Test/runtime utilities
pytest>=8.0
